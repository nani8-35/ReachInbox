import { Inbox, Search } from "lucide-react";
import type { Email } from "../types";
const labels: Record<string, string> = { scheduled: "Scheduled", deferred: "Deferred", sending: "Sending", sent: "Sent", failed: "Failed" };
export function EmailTable({ emails, kind, loading, search, onSearch }: { emails: Email[]; kind: "scheduled" | "sent"; loading: boolean; search: string; onSearch: (v: string) => void }) { const title = kind === "scheduled" ? "Scheduled emails" : "Sent emails"; return <section className="rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="flex flex-col gap-3 border-b border-slate-100 px-5 py-4 sm:flex-row sm:items-center sm:justify-between"><div><h2 className="font-semibold text-ink">{title}</h2><p className="text-sm text-slate-500">{kind === "scheduled" ? "Upcoming and queued deliveries" : "Delivery history"}</p></div><label className="relative"><Search size={16} className="absolute left-3 top-2.5 text-slate-400"/><input value={search} onChange={(e) => onSearch(e.target.value)} placeholder="Search emails" className="w-full rounded-lg border border-slate-200 py-2 pl-9 pr-3 text-sm outline-none focus:border-brand sm:w-56"/></label></div>{loading ? <div className="p-12 text-center text-sm text-slate-500">Loading emails…</div> : emails.length === 0 ? <div className="grid place-items-center gap-2 p-16 text-center"><Inbox size={28} className="text-slate-300"/><p className="font-medium text-slate-700">Nothing here yet</p><p className="text-sm text-slate-500">{kind === "scheduled" ? "Schedule a campaign to see it here." : "Delivered messages will appear here."}</p></div> : <div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead className="bg-slate-50 text-xs uppercase tracking-wide text-slate-500"><tr><th className="px-5 py-3 font-medium">Recipient</th><th className="px-5 py-3 font-medium">Subject</th><th className="px-5 py-3 font-medium">{kind === "sent" ? "Sent time" : "Scheduled time"}</th><th className="px-5 py-3 font-medium">Status</th></tr></thead><tbody>{emails.map((email) => <tr key={email.id} className="border-t border-slate-100"><td className="px-5 py-4 font-medium text-slate-700">{email.recipient}</td><td className="max-w-[260px] truncate px-5 py-4 text-slate-600">{email.subject}</td><td className="whitespace-nowrap px-5 py-4 text-slate-500">{new Date(kind === "sent" ? email.sentAt! : email.scheduledAt).toLocaleString()}</td><td className="px-5 py-4"><span className={`rounded-full px-2.5 py-1 text-xs font-medium ${email.status === "sent" ? "bg-emerald-50 text-emerald-700" : email.status === "failed" ? "bg-red-50 text-red-700" : "bg-amber-50 text-amber-700"}`}>{labels[email.status]}</span></td></tr>)}</tbody></table></div>}</section> }
