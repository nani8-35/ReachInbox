import { LogOut, Mail, PlugZap } from "lucide-react";
import type { User } from "../types";
import { apiUrl } from "../lib/api";
export function Header({ user, slackConnected, onLogout }: { user: User; slackConnected: boolean; onLogout: () => void }) { return <header className="flex items-center justify-between border-b border-slate-100 bg-white px-5 py-3.5 sm:px-9"><div className="flex items-center gap-3 text-ink"><span className="grid h-9 w-9 place-items-center rounded-xl bg-brand text-white"><Mail size={19}/></span><span className="text-lg font-bold tracking-tight">Mailflow</span></div><div className="flex items-center gap-3"><a href={`${apiUrl}/api/slack/connect`} className={`hidden rounded-lg px-3 py-2 text-sm font-medium sm:flex sm:items-center sm:gap-2 ${slackConnected ? "bg-emerald-50 text-emerald-700" : "bg-slate-100 text-slate-700 hover:bg-slate-200"}`}><PlugZap size={16}/>{slackConnected ? "Slack connected" : "Connect Slack"}</a><div className="hidden text-right sm:block"><p className="text-sm font-semibold text-slate-800">{user.name}</p><p className="text-xs text-slate-500">{user.email}</p></div>{user.avatarUrl ? <img className="h-9 w-9 rounded-full object-cover" src={user.avatarUrl} alt="Profile"/> : <span className="grid h-9 w-9 place-items-center rounded-full bg-violet-100 text-sm font-bold text-brand">{user.name[0]}</span>}<button onClick={onLogout} title="Logout" className="rounded-lg p-2 text-slate-500 hover:bg-slate-100 hover:text-slate-800"><LogOut size={18}/></button></div></header> }
